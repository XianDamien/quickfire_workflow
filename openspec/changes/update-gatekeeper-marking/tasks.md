## 1. Implementation
- [x] 1.1 Define a shared `ink` enum and mapping from anomaly note types.
- [x] 1.2 Update gatekeeper execution to never block the pipeline and always return `ink`.
- [x] 1.3 Persist `ink` into `4_llm_annotation.json` for single-run and batch flows.
- [x] 1.4 Include `ink` in API responses that return annotation results.
- [x] 1.5 Update docs/README for the non-blocking gatekeeper behavior and output schema.
- [x] 1.6 Add tests/fixtures to verify `ink` presence and values.
