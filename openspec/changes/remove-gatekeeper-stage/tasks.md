## 1. Implementation
- [ ] 1.1 Remove `gatekeeper` from `DAG_STAGES`, CLI choices, and stage dispatch logic.
- [ ] 1.2 Stop pipeline `ink` propagation while keeping output `ink` defaulted to `normal`.
- [ ] 1.3 Add a standalone gatekeeper entry point so it can be run independently of the main pipeline.
- [ ] 1.4 Update docs/usage text and run a real-audio pipeline to confirm cards complete without gatekeeper.
