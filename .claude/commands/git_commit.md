## context

automatically run git commit.

## task

If it is the first time run git commit 
1. identifying the project language, framework and . write appropriate .gitignore file
2. read git diff , stage necessary files and make commit 
